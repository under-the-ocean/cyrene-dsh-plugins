/**
 * 昔涟 (Cyrene) skin — Host half.
 *
 * The host half is a no-op carrier: this is a pure client plugin. All behavior
 * lives in ./client.js (theme token overrides + global stylesheet).
 */
export function apply() {
  // no-op: pure client plugin.
}
